docker:
  registry: docker.io
  registry_prefix: tetwe
  default_push: true
  expose_port: 9999
  username: testuser
  password: testpass12123
server:
  host: 0.0.0.0
  port: 8000
  username: admin
  password: admin
  default_image_format: '{registry}/{jar_basename}'
  default_template: dragonwell8.Dockerfile
templates:
  enabled: true
  directory: dockerfiles
  default: simple
